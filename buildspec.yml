version: 0.2

phases:
  install:
    commands:
      - echo "Hello, Not Work! (phase=install) "
  pre_build:
    commands:
      - echo "Hello, Not Work! (phase=pre_build)"
  build:
    commands:
      - echo "Hello, World! (phase=build)"
      - ls -al
  post_build:
    commands:
      - echo "Hello, World! (phase=post_build)"